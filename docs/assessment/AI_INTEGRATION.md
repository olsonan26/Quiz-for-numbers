# AI Integration

AI is disabled in V1. There is no provider, secret, model call, prompt, consent toggle, or raw-response transmission.

Any future AI layer must receive minimized structured findings, require separate explicit consent, validate structured output, scan prohibited language, record prompt/model versions, time out safely, and fall back to the complete deterministic report. AI may never change scoring, branching, confidence, contradiction, chart alignment, or safety classification.
